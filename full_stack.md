# 舰C数据拦截全栈方案 (full_stack.md)

> **版本**：v1.0
> **日期**：2026-08-07
> **适用项目**：kc_integrate
> **目标**：从舰C游戏拦截数据 → 脚本推送到后端 → 后端存入数据库

---

## 整体架构

```
舰C游戏页面 → 浏览器脚本拦截数据 → 推送到后端 → 后端处理并存入数据库
```

---

## 第一步：明确要拦截什么数据

先想清楚需要从舰C里拿到什么信息。例如：
- 舰队编成（舰船列表、等级、装备）
- 资源数量（油弹钢铝、桶、喷火器等）
- 远征/入渠状态
- 任务列表

**关键**：舰C的 API 是固定的，每个操作（如进入母港、出击、补给）都会触发对应的 API 请求。需要列出具体要拦截哪些 API 路径。

---

## 第二步：在脚本中识别目标 API

在拦截逻辑里增加过滤条件，只处理关心的 API 请求，忽略其他无关请求。

**注意**：这一步只负责"识别"，不涉及发送数据到后端。

---

## 第三步：解析 API 响应数据

舰C 的 API 响应格式是固定的（通常是 JSON），但数据字段名是缩写的（如 `api_ship_id`、`api_lv` 等）。需要：
- 了解每个 API 返回的字段含义
- 把原始响应解析成需要的结构化数据

---

## 第四步：在脚本中配置后端连接信息

在脚本里预留配置项，包括：
- 后端服务器地址（HTTPS URL）
- 一个验证用的 API Key（防止被别人滥用）

**安全考虑**：这个 API Key 不要硬编码在公开仓库里，可以做成让用户自己填的变量。

---

## 第五步：设计数据推送策略

决定什么时候、怎么把数据发给后端：
- **实时推送**：每次拦截到目标 API 就立即发送
- **批量推送**：缓存一段时间的数据，定期一次性发送
- **去重策略**：同一类 API 短时间内多次触发，是否需要合并或丢弃重复数据

---

## 第六步：搭建后端服务

需要一个能接收 HTTP POST 请求的服务，功能包括：
- 接收脚本发来的数据
- 验证 API Key 是否合法
- 对数据进行格式校验和清洗
- 存入数据库

**技术选型建议**：
- 后端语言：Python / Node.js / Go 都可以
- 数据库：SQLite（轻量）或 PostgreSQL / MySQL（正式）
- 部署：云服务器 / VPS / 容器

---

## 第七步：设计数据库表结构

根据要存的数据类型，设计对应的表。例如：
- `ships` 表：存储舰船信息
- `resources` 表：存储资源数量
- `expeditions` 表：存储远征状态

每张表需要包含：
- 用户标识（区分不同提督的数据）
- 时间戳（记录数据更新时间）
- 具体数据字段

---

## 第八步：后端 API 接口设计

设计一个接收数据的接口，约定好：
- URL 路径
- 请求方法（POST）
- 请求体格式（JSON）
- 认证方式（Header 里带 API Key）
- 返回格式（成功/失败的响应）

---

## 第九步：测试联调

按这个顺序验证：
1. 脚本能否正常拦截到目标 API
2. 解析后的数据格式是否正确
3. 脚本能否成功 POST 到后端
4. 后端能否正确接收并验证数据
5. 数据是否成功写入数据库
6. 全流程走通，没有报错

---

## 第十步：部署与维护

- 后端部署到公网服务器，配置 HTTPS
- 数据库定期备份
- 脚本更新时，注意向后兼容（旧版脚本用户不会立即更新）
- 监控后端日志，发现异常及时处理

---

## 安全注意事项

| 风险 | 防范措施 |
|------|----------|
| API Key 泄露 | 不硬编码在公开仓库；考虑 IP 白名单或请求频率限制 |
| 中间人攻击 | 所有通信强制走 HTTPS |
| 数据隐私 | 后端只存游戏数据，绝不存 DMM Cookie 或登录凭证 |
| 请求频率过高 | 脚本端控制发送频率，后端做限流 |

---

> **最后更新**：2026-08-07
> **维护者**：Qing-Feng
> **仓库**：https://github.com/Qing-Feng-123/kc_integrate
