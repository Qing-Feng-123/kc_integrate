# 舰C平板端插件浏览器 — 实现路径分析

> 基于 PRD.md 的 MVP 到完整生态的渐进式开发路线

---

## 阶段 1：项目启动与环境确认（1-2 天）

**目标**：确定技术栈能跑通，搭出空壳项目。

### 关键任务
- 用 Android Studio 新建 Kotlin + Jetpack Compose 项目，最低版本定在 Android 8.0（API 26）。
- 引入核心依赖：Compose UI、ViewModel、Flow、Room、DataStore。
- 配置网络权限和悬浮窗权限。
- 做第一次真机/模拟器验证：Compose 能正常渲染，项目能打包安装。

### 验收标准
- 空项目能在平板上安装并显示 "Hello World"。
- 确认目标平板的 Android 版本 ≥ minSdk。

### 关键决策
- **是否用 Compose 做窗口管理？** PRD 里选了 Compose，但如果你对自定义拖拽布局不熟悉，也可以先用传统 View 系统（XML + WindowManager）做窗口层，Compose 只做 UI 面板。建议先用 Compose，因为 PRD 明确选了它，且平板大屏适配确实更方便。

---

## 阶段 2：基础套壳浏览器 — "点开即见游戏"（3-5 天）

**目标**：打开 App，一个全屏 WebView 加载 DMM 登录页，用户能正常登录并进入舰C母港。

### 关键任务
1. **WebView 深度配置**
   - 开启 JavaScript、DOM Storage、数据库缓存。
   - 开启缩放支持（双指捏合），但隐藏默认缩放按钮。
   - 允许混合内容（HTTP/HTTPS 混用，DMM 体系内常见）。
   - **最关键一步：User-Agent 伪装**。必须改成桌面版 Chrome 的 UA，否则 DMM 会识别为移动端并拦截或跳转。

2. **沉浸模式**
   - 隐藏系统状态栏和导航栏，全屏显示游戏。
   - 手势滑动可临时呼出系统栏，几秒后自动隐藏。

3. **横竖屏适配**
   - 舰C 本质是横屏游戏，锁定横屏体验最好；但如果要支持竖屏，需要处理 WebView 的 `wideViewport` 和 `overviewMode`，让页面自适应缩放。

### 验收标准
- 打开 App → 看到 DMM 登录页 → 登录成功 → 进入舰C母港。
- 页面占满整个屏幕，无系统栏遮挡。
- 横竖屏切换不崩溃，页面能自适应。

### 风险预警
- **DMM 反检测**：UA 伪装只是第一道防线。如果 DMM 升级检测逻辑（比如检查 `navigator.platform`、屏幕尺寸、触摸事件特征），你可能需要额外注入 JavaScript 来覆盖这些指纹。建议把"反检测参数"做成可配置项，不要写死，方便后续热更新调整。
- **WebView 缓存策略**：舰C 资源体积大，合理配置缓存能显著减少二次启动加载时间。但缓存过多可能导致版本更新后显示旧内容，需要设计"清除缓存"入口。

---

## 阶段 3：窗口管理系统 — "桌面级多窗口"（4-6 天）

**目标**：在 Compose 中实现一个"虚拟桌面"，游戏窗口始终最大化在底层，插件窗口可以浮动、拖动、缩放、最小化。

### 关键任务
1. **窗口状态建模**
   - 每个窗口需要维护：唯一 ID、标题、位置（x, y）、尺寸（width, height）、层级（z-index）、最大化/最小化/停靠状态。
   - 建议用百分比坐标（相对于屏幕宽高的比例），而不是绝对像素，这样在不同分辨率平板上都能正常还原位置。

2. **游戏窗口的特殊处理**
   - 初始状态：最大化，占满整个桌面。
   - 标题栏：精简，可自动隐藏（比如顶部一条细线，点击后展开）。
   - 可以缩小，但不能关闭。
   - 当游戏窗口最大化时，其他插件窗口需要自动避让（缩为侧边条或最小化）。

3. **插件窗口的通用行为**
   - 拖动：按住标题栏任意拖动，靠近屏幕边缘时自动吸附停靠。
   - 缩放：从边框或四角拖动改变大小。
   - 最小化：缩成任务栏图标或侧边细条，点击可恢复。
   - 置顶：点击任意窗口，该窗口自动提升到最高层级。

4. **Compose 自定义布局**
   - 用 `Box` 作为桌面容器，所有窗口作为 `Box` 的子项，通过 `offset` 和 `size` 控制位置。
   - 层级用 `zIndex` 控制。
   - 拖拽逻辑需要处理触摸事件（`pointerInput`），计算手指移动 delta 并更新窗口位置。

### 验收标准
- 游戏窗口全屏显示，有一个"测试插件窗口"浮动在游戏上方。
- 能拖动插件窗口到屏幕任意位置。
- 点击游戏区域，游戏能正常响应点击；点击插件窗口，插件窗口置顶。
- 插件窗口可以最小化成一个细条，点击恢复。

### 关键决策
- **窗口管理用 Compose 还是原生 WindowManager？**
  - Compose 方案：所有窗口在一个 Activity 内，共享生命周期，内存管理简单，但拖拽/缩放需要自己实现手势逻辑。
  - WindowManager 方案：每个插件窗口是一个独立的系统悬浮窗（`TYPE_APPLICATION_OVERLAY`），可以真正独立于主窗口，但权限申请复杂，且与 Compose 的声明式范式不太搭。
  - **建议**：先用 Compose 单 Activity 方案做 MVP，等后期需要插件"跨应用显示"时再考虑 WindowManager。

---

## 阶段 4：数据拦截层 — "游戏与插件的桥梁"（5-7 天）

**目标**：游戏 WebView 里的 API 请求（如 `api_port`）被拦截下来，解析后通过数据总线分发给插件。

### 关键任务
1. **JS 注入拦截器**
   - 在游戏页面加载完成后，向 WebView 注入一段 JavaScript。
   - 这段脚本劫持原生的 `XMLHttpRequest` 和 `fetch`，在请求返回时把 URL 和响应体截获，通过 `JavascriptInterface` 桥接到 Kotlin 层。
   - **核心原则**：只读取，不修改。拦截后原响应必须正常返回给游戏，否则游戏会卡死或报错。

2. **Kotlin 数据总线**
   - 收到 JS 传来的数据后，按 API 路径分类缓存。
   - 提供两种消费方式：
     - **实时推送**：用 Kotlin Flow 的 `SharedFlow`，插件订阅某个 API 路径，有新数据时自动收到。
     - **主动查询**：插件随时获取某个 API 的最新缓存数据。

3. **舰C 专用数据解析**
   - `api_port` 是最核心的 API，包含舰队、舰娘、装备、资源等全部母港信息。
   - 需要把 JSON 解析成结构化对象（舰队列表、舰娘详情、装备属性等），供插件直接调用。
   - 解析层要模块化，舰C 的 API 格式偶尔会变动，未来需要能热更新解析逻辑。

### 验收标准
- 打开游戏进入母港，Kotlin 层能成功打印出 `api_port` 的响应内容。
- 数据总线能缓存至少最近一次的所有 API 响应。
- 一个测试接口（如"获取第一舰队数据"）能正确返回解析后的舰娘列表。

### 风险预警
- **性能问题**：如果每个 API 响应都完整字符串传递，数据量大时可能造成主线程卡顿。建议拦截后先在 JS 层做简单过滤，只把关键 API（如 `api_port`、`api_req_sortie` 等）传回 Kotlin。
- **时序问题**：插件可能在数据到达前就发起了查询，需要处理"数据尚未就绪"的返回逻辑（返回空或默认值）。

---

## 阶段 5：插件系统 — "让第三方逻辑跑起来"（5-7 天）

**目标**：定义插件格式，实现安装、加载、沙箱运行，并提供预置 API 供插件调用。

### 关键任务
1. **插件包格式定义**
   - 采用 ZIP 包，内部包含 `manifest.json`（配置信息）和入口文件（HTML/JS/CSS/图片资源）。
   - `manifest.json` 需要声明：名称、版本、作者、入口文件、默认窗口尺寸/位置、需要的权限、自动加载的 URL 匹配规则。

2. **插件安装与管理**
   - 用户从文件管理器选择 ZIP 文件，App 解压到私有目录。
   - 维护一个已安装插件列表，支持启用/禁用/卸载。
   - 禁用插件时销毁其窗口但保留文件；卸载时删除目录。

3. **插件沙箱 WebView**
   - 每个插件运行在独立的 WebView 中，加载本地文件。
   - 沙箱内预置一个全局对象（如 `PluginData`），提供以下能力：
     - 读取舰队/舰娘数据
     - 订阅特定 API 的数据更新
     - 读写插件私有配置
     - 输出调试日志
     - 显示/隐藏加载状态

4. **窗口联动**
   - 插件启用时，根据 `manifest` 中的默认尺寸和位置创建浮动窗口。
   - 插件窗口与游戏窗口共用同一套窗口管理系统（拖动、缩放、最小化、置顶）。

### 验收标准
- 用户能导入一个测试插件 ZIP，App 正确解析 `manifest`。
- 插件窗口显示"Hello World"并浮动在游戏上方。
- 插件能通过预置 API 读取到第一舰队的舰娘名称和等级。
- 禁用插件后窗口消失，重新启用后恢复。

### 关键决策
- **插件权限模型**：PRD 里提到了 `data_read`、`storage`、`notification`、`network`。初期建议只开放 `data_read` 和 `storage`，`network` 和 `notification` 等涉及安全和系统资源的权限后续再评估。
- **插件间通信**：PRD 的"后续待决策事项"里提到了这一点。MVP 阶段建议**明确禁止**插件间直接通信，避免数据污染和安全风险。每个插件只能与数据总线交互。

---

## 阶段 6：Tampermonkey 支持 — "快速拥有插件生态"（3-4 天）

**目标**：让用户导入现有的 `.js` 用户脚本，无需等待官方适配插件。

### 关键任务
1. **GM_API 映射**
   - 在插件沙箱中预置 Tampermonkey 常用的 GM 函数：
     - `GM_xmlhttpRequest` → 桥接到系统网络层（或直接用 WebView 的 fetch）。
     - `GM_setValue` / `GM_getValue` → 桥接到 DataStore。
     - `GM_notification` → 系统通知。
     - `GM_addStyle` → 动态注入 CSS。
   - 不需要 100% 兼容所有 GM API，先覆盖舰C 玩家最常用的 5-6 个即可。

2. **脚本导入流程**
   - 用户从 GreasyFork 或其他渠道下载 `.js` 文件，通过文件选择器导入 App。
   - App 把脚本内容存入数据库，用户可以在列表中启用/禁用。
   - 启用的脚本在插件沙箱中执行，可以创建浮动面板（通过调用扩展 API）。

3. **脚本与插件的共存**
   - 一个脚本本质上是一个"无 manifest 的轻量插件"，可以共用插件窗口系统。
   - 脚本创建的浮动面板通过统一的 `PluginUI` API 申请窗口。

### 验收标准
- 用户导入一个现有的舰C 相关 Tampermonkey 脚本（如战斗预测或舰队面板脚本）。
- 脚本能正常运行，至少核心功能可用。
- 脚本数据（如 `GM_setValue` 存储的内容）能持久化保存。

---

## 阶段 7：状态持久化与保活（2-3 天）

**目标**：App 退出后下次打开能恢复窗口布局；游戏在后台时不被系统杀死。

### 关键任务
1. **窗口状态保存**
   - App 退出时（`onStop` 或 `onDestroy`），把每个窗口的：插件 ID、位置、尺寸、状态（最大化/正常/最小化/停靠）、停靠边缘、层级顺序，全部写入 Room 数据库。
   - 启动时读取并重建所有窗口。

2. **后台保活**
   - 启动一个前台服务（Foreground Service），挂一个持续通知，防止系统因内存不足杀死进程。
   - 舰C 本身需要持续网络连接来处理远征、入渠等计时，保活对用户体验很重要。

### 验收标准
- 打开两个插件窗口，调整位置和大小，杀掉 App 进程，重新打开，窗口位置和状态完全恢复。
- App 切换到后台 10 分钟后，重新切回前台，游戏仍在运行，未掉线。

---

## 推荐开发顺序（MVP 冲刺版）

| 时间 | 阶段 | 交付物 |
|---|---|---|
| 第 1 周 | 阶段 1 + 阶段 2 | 能打开 App，看到舰C母港 |
| 第 2 周 | 阶段 3 | 游戏全屏 + 一个可拖动的测试窗口 |
| 第 3 周 | 阶段 4 | 数据拦截成功，Kotlin 层能拿到 `api_port` |
| 第 4 周 | 阶段 5 前半 | 插件 ZIP 安装、沙箱加载、预置 API 可用 |
| 第 5 周 | 阶段 5 后半 + 阶段 6 | 官方适配"舰队面板"插件 + Tampermonkey 脚本导入 |
| 第 6 周 | 阶段 7 | 窗口状态持久化 + 后台保活 |

---

## 全局风险与应对策略

| 风险 | 影响 | 应对 |
|---|---|---|
| **DMM 反检测升级** | 无法登录或游戏闪退 | 把 UA、指纹参数全部配置化，不硬编码，后续可通过热更新或远程配置调整 |
| **WebView 性能瓶颈** | 多窗口卡顿、发热 | 限制同时运行的插件数量（建议 ≤3）；游戏和插件 WebView 按需创建，最小化时暂停渲染 |
| **舰C API 格式变更** | 数据解析错误，插件显示异常 | 解析层完全模块化，每个 API 一个独立解析器，支持远程热更新解析规则 |
| **插件兼容性问题** | 部分脚本/插件无法运行 | MVP 阶段明确支持范围，提供"兼容清单"；长期提供移植指南和 API 对照表 |
| **平板分辨率碎片化** | 窗口位置在不同设备上错乱 | 所有坐标用百分比，不用绝对像素；停靠逻辑基于屏幕边缘检测 |
| **后台被杀导致掉线** | 远征/入渠中断 | 前台服务 + 系统电池优化白名单引导（让用户手动设置） |

---

## 下一步建议

1. **先验证阶段 2**：花 2-3 天把 WebView 跑起来，确认能登录 DMM 进母港。这是整个项目的生死线，如果这一步卡住了（比如 DMM 检测太严），后面的架构都无从谈起。
2. **阶段 3 和阶段 4 可以并行设计**：窗口管理和数据拦截没有强依赖，可以两个分支同时推进。
3. **阶段 5 的插件系统不要一上来就追求完美**：先做一个"硬编码"的测试插件，确认数据能正确显示在浮动窗口里，再去做通用的 ZIP 解析和沙箱加载。
