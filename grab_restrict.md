# 舰C数据拦截安全设计原则 (grab_restrict.md)

> **版本**：v1.0
> **日期**：2026-08-06
> **适用项目**：舰C平板端插件浏览器 (kc_integrate)
> **核心目标**：在零封号风险的前提下，实现游戏数据的被动采集与跨设备展示

---

## 一、项目背景

### 1.1 项目定位
本项目旨在通过浏览器端用户脚本（Tampermonkey）拦截《舰队Collection》（舰C）的 API 响应数据，推送至自建云端后端，实现跨设备状态监控与辅助提醒。

### 1.2 技术路线
- **采集端**：Edge 浏览器 + Tampermonkey 扩展（平板端）
- **传输层**：HTTPS POST 到自建后端
- **存储层**：Flask + SQLite（云端）
- **展示层**：独立前端页面（手机/PC 浏览器）

### 1.3 安全前提
舰C官方对第三方工具的态度是：**禁止主动操作类工具**（机器人、宏、加速），但对**纯被动数据监听工具**（如 poi、KC3 Kai、航海日志）历史上无任何封号记录。

---

## 二、业界工具安全等级综述

根据舰C中文社区经典分析及 Wiki 记载，第三方工具按风险分为以下几类：

| 等级 | 工具类型 | 代表 | 检测方式 | 风险系数 |
|:---:|---|---|:---|:---:|
| 🟢 | **无主动行为的浏览器插件** | poi, KC3 Kai, 航海日志, 提督很忙 | 官方无检测手段 | ★ |
| 🟡 | 修改客户端资源（立绘/语音替换） | 魔改包 | 理论上可检测文件 MD5，目前未部署 | ★★ |
| 🟠 | 加速游戏运行 | 变速齿轮 | 发包频率明显高于平均值即可确认 | ★★★ |
| 🔴 | 自动程序（模拟操作） | 自动远征、自动出击脚本 | 连续24小时不间断游戏、发包频率异常 | ★★★★ |
| 🔴 | 直接攻击服务器 | 模拟封包、伪造请求 | 服务器端直接识别异常请求 | ★★★★★ |

> **结论**：纯被动监听类工具（本项目所属类别）在业界有十年以上的安全使用记录，风险极低。

---

## 三、安全设计铁律（三不原则）

### 3.1 只读不写
- ✅ **允许**：读取 `XMLHttpRequest` 的 `responseText`，解析 JSON 数据
- ❌ **禁止**：修改任何请求参数、响应内容、DOM 元素以影响游戏逻辑

```javascript
// ✅ 安全示例
const data = JSON.parse(xhr.responseText);
console.log('[拦截]', data);

// ❌ 危险示例（绝对禁止）
xhr.responseText = '{"api_hp": 99999}'; // 修改游戏数据 = 作弊
```

### 3.2 只发外部，不发游戏服务器
- ✅ **允许**：将解析后的数据通过 HTTPS POST 发送到自建后端
- ❌ **禁止**：向 DMM/舰C 游戏服务器发送任何伪造或重放的请求

```javascript
// ✅ 安全示例
fetch('https://your-backend.com/api/fleet', {
    method: 'POST',
    body: JSON.stringify(parsedData)
});

// ❌ 危险示例（绝对禁止）
fetch('https://osapi.dmm.com/kcsapi/api_req_sortie/...', {
    method: 'POST',
    body: '伪造的请求体'
}); // 模拟封包 = 直接攻击服务器
```

### 3.3 不模拟任何用户操作
- ✅ **允许**：在游戏自然产生的 API 请求触发时执行被动监听
- ❌ **禁止**：自动点击按钮、自动提交表单、自动发送游戏内指令

```javascript
// ❌ 绝对禁止（封号高危行为）
document.querySelector('.start-expedition').click();
document.querySelector('.api-start').dispatchEvent(new Event('click'));
```

---

## 四、技术实现约束

### 4.1 拦截层约束
| 约束项 | 要求 | 理由 |
|---|---|---|
| 拦截时机 | `document-start`（页面加载前注入） | 确保能覆盖游戏初始化请求 |
| 拦截对象 | 仅覆盖 `XMLHttpRequest` 和 `fetch` | 不触碰游戏其他运行时 |
| 响应处理 | 只读 `responseText`，不修改 | 避免影响游戏正常渲染 |
| 异常处理 | 所有拦截逻辑必须 `try-catch` 包裹 | 防止脚本错误导致游戏崩溃 |

### 4.2 传输层约束
| 约束项 | 要求 | 理由 |
|---|---|---|
| 协议 | 仅 HTTPS | 防止中间人攻击 |
| 频率 | 被动触发，不主动轮询 | 避免产生异常流量特征 |
| 数据内容 | 仅发送解析后的 JSON 数据 | 不发送 Cookie、Token 等敏感凭证 |
| 失败处理 | 发送失败静默丢弃，不重试 | 避免重试风暴 |

### 4.3 后端层约束
| 约束项 | 要求 | 理由 |
|---|---|---|
| 认证 | 使用独立 API Key，不依赖游戏 Cookie | 防止后端被利用攻击游戏 |
| 存储 | 仅存储游戏数据，不存储账号密码 | 最小数据原则 |
| 访问控制 | 前端查询需验证，防止数据泄露 | 保护用户隐私 |

---

## 五、风险自检清单

在每次脚本更新或功能扩展前，必须确认以下检查项：

- [ ] 脚本是否只读取响应，不修改任何请求/响应内容？
- [ ] 脚本是否不向 DMM/舰C 服务器发送任何请求？
- [ ] 脚本是否不模拟点击、输入等用户操作？
- [ ] 脚本是否只在游戏自然发请求时触发，不产生额外请求？
- [ ] 后端是否不存储用户的 DMM Cookie 或登录凭证？
- [ ] 新增功能是否属于"被动展示"而非"主动干预"？

**如任一答案为"否"，必须重新设计该功能。**

---

## 六、参考与引用

1. **Kancolle Wiki - 3rd Party Software**
   > "Outside of Bots and the use of macro, no ban has been recorded regarding the usage of such software."

2. **舰C中文社区 - 第三方工具安全性分析**
   > "无主动行为的第三方浏览器及插件...官方没有任何检测手段，风险系数：★"

3. **开源项目参考**
   - `poooi/poi` - Electron 本地代理方案（MIT 协议）
   - `KC3Kai/KC3Kai` - Chrome 扩展被动拦截方案
   - `antest1/kcanotify` - Android VPN 抓包方案（GPL v3）

---

## 七、免责声明

本项目仅供技术学习与研究使用。使用者应自行承担风险，遵守《舰队Collection》服务条款。开发者不鼓励、不支持任何违反游戏规则的行为，包括但不限于自动操作、模拟封包、游戏加速等。

---

> **最后更新**：2026-08-06
> **维护者**：Qing-Feng
> **仓库**：https://github.com/Qing-Feng-123/kc_integrate
