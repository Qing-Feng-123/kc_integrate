# 舰C平板端插件浏览器 PRD

## 1. 产品定位

**一句话描述**：在Android平板上运行舰C，同时支持加载桌面端浏览器插件（poi/KC3/Tampermonkey），让平板成为"带插件的游戏专用设备"。

**核心优势**：
- 大屏：比手机显示更多内容
- 独立：不占用笔记本，边玩边做其他事
- 插件：复用桌面端丰富的玩家插件生态

---

## 2. 用户场景

| 场景 | 用户行为 | 期望结果 |
|------|---------|---------|
| 日常出击 | 打开App，登录游戏，加载舰队面板插件 | 边玩边看舰队状态 |
| 活动攻略 | 加载战斗预测插件 + 带路条件插件 | 实时判断路线和胜率 |
| 远征管理 | 加载远征计时插件，最小化到侧边 | 后台提醒，不遮挡游戏 |
| 多插件并行 | 同时打开制空值、任务追踪、装备改修插件 | 像桌面一样多窗口排列 |

---

## 3. 核心架构

```
┌─────────────────────────────────────────┐
│              桌面窗口管理器                │
│         (Desktop Window Manager)         │
│                                         │
│  ┌─────────────┐  ┌─────────────┐      │
│  │  游戏窗口    │  │  插件窗口A   │      │
│  │  (WebView)  │  │  (WebView)   │      │
│  │  默认最大化  │  │  浮动/停靠   │      │
│  │  可缩小拖动  │  │  可缩放拖动  │      │
│  └─────────────┘  └─────────────┘      │
│                                         │
│  所有窗口平等：都能拖动、缩放、最小化、停靠  │
│                                         │
└─────────────────────────────────────────┘
                    │
    ┌───────────────┼───────────────┐
    ▼               ▼               ▼
┌─────────┐   ┌─────────┐   ┌─────────┐
│数据拦截层 │   │插件沙箱层 │   │面板渲染层 │
│(JS Bridge)│   │(独立WebView)│   │(WindowManager)│
└─────────┘   └─────────┘   └─────────┘
```

---

## 4. 技术选型（已确认）

| 层级 | 选型 | 理由 |
|------|------|------|
| 开发语言 | Kotlin | Android官方，协程处理异步 |
| UI框架 | Jetpack Compose | 声明式UI，平板大屏适配好 |
| 游戏/插件容器 | Android WebView | 系统自带，统一架构 |
| 数据存储 | Room + DataStore | 结构化数据 + 键值配置 |
| 跨层通信 | Kotlin Flow | 响应式数据流 |
| 悬浮窗 | WindowManager + TYPE_APPLICATION_OVERLAY | 系统级覆盖 |
| 后台保活 | Foreground Service | 持续处理插件数据 |
| 插件格式 | ZIP (manifest.json + 资源) | 兼容Chrome Extension格式 |

---

## 5. 窗口系统

### 5.1 游戏窗口

| 属性 | 设定 |
|------|------|
| 初始状态 | 最大化，占满桌面 |
| 标题栏 | 精简（拖动区 + 最小化按钮），可自动隐藏 |
| 缩放 | 支持双指捏合、边框拖动 |
| 关闭 | 不可关闭，只能最小化 |
| 特殊能力 | 注入JS拦截网络数据，推送到数据总线 |

### 5.2 插件窗口

| 属性 | 设定 |
|------|------|
| 初始状态 | 按manifest中defaultWidth/Height/Position |
| 标题栏 | 完整（图标 + 名称 + 最小化/最大化/关闭） |
| 缩放 | 支持边框拖动、角落拖动 |
| 最小化 | 缩为任务栏图标或侧边吸附条 |
| 停靠 | 贴左/右/上/下边缘，自动调整尺寸 |

### 5.3 窗口交互

- **点击窗口任意位置**：该窗口置顶（z-index最高）
- **拖动标题栏**：自由移动，靠近边缘自动吸附
- **双击标题栏**：切换最大化/正常
- **游戏窗口最大化时**：插件自动避让（缩为侧边条或最小化）

---

## 6. 插件系统

### 6.1 插件格式

```
plugin.zip
├── manifest.json          # 插件配置
├── index.html             # 入口页面
├── main.js                # 核心逻辑
├── style.css              # 样式
└── assets/                # 图片等资源
```

**manifest.json 关键字段**：

| 字段 | 说明 |
|------|------|
| `name` / `version` / `author` | 基本信息 |
| `url_matches` | 自动加载的URL匹配规则（如 `*.dmm.com/*`） |
| `permissions` | 声明需要的权限（data_read, storage, notification, network） |
| `window` | 默认尺寸、位置、是否可缩放/最小化/停靠 |
| `entry` | 入口文件路径 |

### 6.2 插件API（预置环境）

插件沙箱内预置全局对象，提供以下能力：

| API | 功能 |
|------|------|
| `PluginData.getFleet(index)` | 获取指定舰队数据 |
| `PluginData.getShip(id)` | 获取舰娘详情 |
| `PluginData.subscribe(apiPath, callback)` | 订阅某类数据更新 |
| `PluginData.getLast(apiPath)` | 获取最近一次某API数据 |
| `PluginConfig.get/set(key, value)` | 读写插件配置 |
| `PluginUI.showLoading/hideLoading()` | 显示/隐藏加载状态 |
| `PluginHost.log(message)` | 输出调试日志 |

### 6.3 插件生命周期

```
未安装 → 安装（解压到私有目录）→ 已禁用
                          ↓
                    用户启用
                          ↓
                    创建沙箱WebView + 窗口
                          ↓
                    运行中（正常/最小化/停靠）
                          ↓
                    用户禁用/关闭 → 销毁窗口，保留文件
```

---

## 7. 数据流

### 7.1 游戏数据拦截

```
游戏WebView发起API请求
        │
        ▼
注入的JS拦截器捕获响应（axios interceptor）
        │
        ▼
JavascriptInterface桥接到Kotlin层
        │
        ▼
数据总线解析、分类、缓存
        │
        ▼
推送给订阅了该API的插件沙箱
        │
        ▼
插件JS接收数据，更新自身UI
```

**关键原则**：只读取，不修改，原响应正常返回给游戏。

### 7.2 插件主动获取数据

```
用户点击插件按钮
        │
        ▼
插件JS调用 PluginData.getFleet(0)
        │
        ▼
沙箱预置API从数据总线读取最新缓存
        │
        ▼
返回舰队数据对象给插件JS
        │
        ▼
插件JS计算，更新自身DOM
```

---

## 8. 插件生态策略

### 8.1 三阶段路线

| 阶段 | 目标 | 实现方式 |
|------|------|---------|
| **短期** | 用户立刻有插件可用 | 内置Tampermonkey运行时，支持导入现有.js脚本 |
| **中期** | 核心功能插件体验完整 | 官方适配5-10个热门poi插件（舰队面板、战斗预测、远征计时等） |
| **长期** | 最大化复用现有生态 | 完善mini-Electron兼容层，支持更多poi插件直接运行 |

### 8.2 Tampermonkey支持（短期）

- 实现主流GM_API映射：
  - `GM_xmlhttpRequest` → 系统网络层
  - `GM_setValue` / `GM_getValue` → DataStore
  - `GM_notification` → 系统通知
  - `GM_addStyle` → 注入CSS
- 用户从GreasyFork下载.js文件，导入App即可使用
- 脚本可创建浮动面板（通过扩展API）

### 8.3 poi插件移植（中期）

- 提供移植指南和API对照表
- 提供模板代码生成工具
- 官方优先适配：舰队信息面板、战斗预测、远征计时、任务追踪、装备改修

---

## 9. 窗口状态与持久化

### 9.1 保存内容

App退出时保存：
- 每个窗口的插件ID
- 位置（x, y）
- 尺寸（width, height）
- 状态（最大化/正常/最小化/停靠）
- 停靠边缘（如适用）
- z-index顺序

### 9.2 恢复逻辑

App启动时：
- 读取保存的状态
- 重建游戏窗口（最大化）
- 按顺序重建插件窗口
- 恢复各窗口位置、尺寸、状态

---

## 10. 最小可行产品（MVP）

### MVP功能范围

| 模块 | 功能 | 优先级 |
|------|------|--------|
| 基础浏览器 | WebView打开DMM，登录舰C，UA伪装 | P0 |
| 数据拦截 | 注入JS拦截api_port，解析舰队数据 | P0 |
| 窗口系统 | 游戏窗口（最大化+可缩小），一个插件窗口（浮动+拖动） | P0 |
| 插件系统 | 加载一个测试插件，显示"Hello World" | P0 |
| 数据API | PluginData.getFleet() 可用 | P0 |
| 完整插件 | 官方适配"舰队信息面板"插件，显示舰娘列表 | P1 |
| TM支持 | 导入一个现有TM脚本运行 | P1 |
| 多插件 | 同时加载2个插件窗口 | P2 |
| 停靠/最小化 | 侧边吸附条、任务栏 | P2 |

### MVP验收标准

1. 打开App，进入舰C母港
2. 安装并启用"舰队面板"插件
3. 插件窗口显示第一舰队舰娘名称和等级
4. 拖动插件窗口到游戏画面右侧
5. 点击游戏画面，事件正常响应
6. 插件数据与游戏同步

---

## 11. 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| DMM更新反检测机制 | 无法登录 | UA和行为配置化，可快速调整 |
| 舰C API格式变化 | 数据解析错误 | 解析层模块化，热更新修复 |
| 插件兼容性问题 | 部分插件无法运行 | 明确支持范围，提供移植工具 |
| WebView性能瓶颈 | 多窗口卡顿 | 限制同时运行插件数量，优化内存 |
| 用户期望过高 | 认为所有插件都能直接用 | 产品说明清晰，分阶段公布能力 |

---

## 12. 后续待决策事项

| 事项 | 说明 | 建议决策时机 |
|------|------|-----------|
| 是否支持插件间通信 | 插件A的数据能否被插件B读取 | 中期，有实际需求后 |
| 是否支持插件热更新 | 不重启App更新插件代码 | 长期，稳定性优先 |
| 是否建立官方插件商店 | 集中分发、审核插件 | 中期，生态有一定规模后 |
| 是否支持iPad | 技术栈完全不同 | 长期，Android验证成功后 |
