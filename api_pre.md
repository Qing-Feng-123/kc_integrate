# API 预处理清单 —— 资源变动场景穷尽分析

> **用途**：供脚本开发参考，明确每个资源变动场景需要拦截哪些 API、读取哪些字段、存在什么难点。  
> **资源定义**：基于 `Resource_Account.md` 的 8 项资源 —— 燃料、弹药、钢材、铝土、喷火器、桶、开发资材、螺丝。

---

## 一、直接支出类（有明确 API，资源直接扣减）

### 1. 通常建造
- **游戏 UI**：工厂 → 建造 → 输入油/弹/钢/铝 → 点击"建造开始"
- **触发 API**：`api_req_kousyou/createship`（**请求体**）
- **消耗**：燃料↓、弹药↓、钢材↓、铝土↓
- **脚本难点**：⚠️ 需读取 **请求体**，响应中不含投入资源数据

### 2. 大建
- **游戏 UI**：工厂 → 勾选"大型舰建造" → 输入资源 → 点击"建造开始"
- **触发 API**：`api_req_kousyou/createship`（`api_large_flag=1`）
- **消耗**：燃料↓、弹药↓、钢材↓、铝土↓（消耗量远高于通常建造）
- **备注**：与通常建造是同一个 API，仅参数不同

### 3. 高速建造（喷火器消耗）
- **游戏 UI**：工厂 → 建造渠显示"建造中" → 点击"高速建造"
- **触发 API**：`api_req_kousyou/createship_speedchange`
- **消耗**：**喷火器↓**（固定消耗 1 个）
- **脚本可读性**：✅ 消耗量固定，无需读取响应

### 4. 装备开发
- **游戏 UI**：工厂 → 开发 → 输入油/弹/钢/铝 → 点击"开发开始"
- **触发 API**：`api_req_kousyou/createitem`（**请求体 + 响应**）
- **消耗**：燃料↓、弹药↓、钢材↓、铝土↓
- **脚本难点**：⚠️ 需同时读取 **请求体**（投入资源）和 **响应**（成败 + 出货装备）

### 5. 舰娘解体
- **游戏 UI**：母港/工厂 → 选择舰娘 → 点击"解体" → 确认
- **触发 API**：`api_req_kousyou/destroyship`（**响应**）
- **收入**：燃料↑、弹药↑、钢材↑、铝土↑（回收，属于收入）
- **脚本可读性**：✅ 响应直接返回 `api_material[]` 回收量
- **额外难点**：响应中的 `api_ship_id` 是**实例 ID**，需配合本地缓存的"实例 ID → 图鉴 ID"映射表才能写入有意义的记录

### 6. 装备废弃
- **游戏 UI**：母港 → 装备 → 选择装备 → 点击"废弃"
- **触发 API**：`api_req_kousyou/destroyitem2`
- **变动**：无 4 主资源变动（不回收资源，仅清理背包）
- **备注**：Resource_Account 未专门记录，可忽略

### 7. 入渠修理
- **游戏 UI**：母港 → 选择受损舰娘 → 点击"入渠" → 选择渠位 → 确认
- **触发 API**：`api_req_nyukyo/start`（**请求体**）
- **消耗**：燃料↓、弹药↓、钢材↓
- **脚本难点**：❌ 响应为空或极简，**消耗量不直接返回**

### 8. 高速修复（桶消耗）
- **游戏 UI**：入渠界面 → 点击"高速修复"
- **触发 API**：`api_req_nyukyo/speedchange`（**请求体**）
- **消耗**：**桶↓**（固定消耗 1 个）
- **脚本可读性**：✅ 消耗量固定

### 9. 舰队补给
- **游戏 UI**：母港 → 选择舰队 → 点击"补给" → 确认
- **触发 API**：`api_req_hokyu/charge`（**响应**）
- **消耗**：燃料↓、弹药↓
- **脚本难点**：❌ 响应通常只返回补给后状态，**不直接返回消耗量**
- **额外注意**：若勾选"艦載機も補充する"，还会消耗**铝土↓**

### 10. 舰载机补充（铝土消耗）
- **游戏 UI**：出击归来后，舰娘搭载舰载机不满 → 回港自动补充 / 手动补给时勾选舰载机补充
- **触发 API**：**无独立 API**，消耗体现在 `api_port/port` 或 `api_req_hokyu/charge` 后的资源变化中
- **消耗**：**铝土↓**
- **脚本难点**：❌ 无独立 API 通知，**必须通过前后快照差值推算**

### 11. 出击出发
- **游戏 UI**：出击选择 → 选择海域 → 选择舰队 → 点击"出击"
- **触发 API**：`api_req_map/start`（**响应**）
- **消耗**：燃料↓、弹药↓（出发时扣除）
- **脚本难点**：❌ 消耗量不直接返回，需根据舰队编成 + 地图推算

### 12. 舰娘改造
- **游戏 UI**：母港 → 舰娘 → 改造 → 确认
- **触发 API**：`api_req_kaisou/kaisou`
- **消耗**：燃料↓、弹药↓、钢材↓、铝土↓（某些改造）
- **脚本可读性**：⚠️ 文档未完整覆盖响应结构，消耗量可能需通过快照差值确认

### 13. 装备改修
- **游戏 UI**：明石工厂 → 选择装备 → 改修 → 确认
- **触发 API**：`api_req_kaisou/remodeling`
- **消耗**：**螺丝↓**、燃料↓/弹药↓/钢材↓/铝土↓（视装备而定）
- **脚本可读性**：⚠️ 文档未完整覆盖响应结构

### 14. 演习
- **游戏 UI**：演习 → 选择对手 → 出击
- **触发 API**：`api_req_practice/battle` 等
- **消耗**：燃料↓、弹药↓（少量）
- **脚本难点**：❌ 消耗量不直接返回

---

## 二、直接收入类（有明确 API，资源直接增加）

### 15. 远征归来（成功）
- **游戏 UI**：母港 → 远征归来的舰队显示"远征归来" → 点击进入结果页
- **触发 API**：`api_req_mission/result`（**响应**）
- **收入**：燃料↑、弹药↑、钢材↑、铝土↑、喷火器↑、桶↑、开发资材↑、螺丝↑
- **脚本可读性**：✅ 响应 `api_material[]` 直接返回 8 项资源收益

### 16. 远征归来（大成功）
- **游戏 UI**：同上，但结果显示"大成功"
- **触发 API**：`api_req_mission/result`（`api_clear_result=2`）
- **收入**：同上，但各项数值更高

### 17. 任务报酬
- **游戏 UI**：任务界面 → 点击"受領"
- **触发 API**：`api_req_quest/clearitemget`（**响应**）
- **收入**：8 项资源中任意组合↑
- **脚本可读性**：✅ 响应 `api_material[]` 和 `api_bounus[]` 直接给奖励

### 18. 自然回复
- **游戏 UI**：无（被动发生）
- **触发 API**：**无 API 通知**
- **收入**：燃料↑、弹药↑、钢材↑、铝土↑（自然回复上限内，每小时 3 点）
- **脚本难点**：❌ 无 API，**必须通过日终对账推算**

---

## 三、间接/推算类（无直接 API 或需多 API 对比）

### 19. 补给精确消耗量
- **问题**：`api_req_hokyu/charge` 不返回消耗量，且可能包含舰载机补充的铝土消耗
- **推算方式**：
  - 方案 A：补给前 `api_port/port` 快照 vs 补给后 `api_port/port` 快照，差值 = 消耗
  - 方案 B：从 `api_get_member/ship` 中各舰 `api_fuel`/`api_bull` 字段变化推算
- **平板端可行性**：❌ 困难（前后快照在内存中活不到对比时）

### 20. 入渠精确消耗量
- **问题**：`api_req_nyukyo/start` 不返回消耗量
- **推算方式**：
  - 入渠前 `api_port/port` 快照 vs 入渠后 `api_port/port` 快照
  - 或从 `api_get_member/ndock` 的 `api_item1/2/3` 读取
- **平板端可行性**：❌ 困难

### 21. 出击途中总消耗
- **问题**：出发时扣一波 + 战斗中持续消耗 + 舰载机击坠后补充消耗铝土
- **推算方式**：出击前 `api_port/port` 快照 vs 回港后 `api_port/port` 快照，减去已知收入（远征、任务等）
- **平板端可行性**：❌ 几乎不可行（出击过程页面刷新频繁，快照保不住）

### 22. 战斗中的铝土消耗（舰载机补充）
- **问题**：舰载机被击坠后，回港自动补充时扣铝土，无独立 API
- **推算方式**：只能通过 `api_port/port` 或 `api_get_member/ship` 中舰载机搭载数变化反推
- **平板端可行性**：❌ 不可行

### 23. 改造/改修精确消耗
- **问题**：文档 3.5 节被截断，未给出 `kaisou`/`remodeling` 响应中的资源字段详情
- **推算方式**：需通过改造前/后 `api_port/port` 快照差值，或解析响应中的隐藏字段
- **平板端可行性**：❓ 不确定

### 24. 装备变更导致的铝土消耗
- **UI**：母港 → 舰娘 → 更换舰载机装备
- **API**：`api_req_kaisou/slotset`
- **问题**：若新搭载的舰载机搭载数大于当前剩余数，系统可能自动补充，消耗铝土
- **推算方式**：需结合 `api_get_member/ship` 中 `api_onslot[]` 变化判断
- **平板端可行性**：❌ 不可行

### 25. 排名奖励
- **UI**：无（每月排名结算自动发放）
- **API**：**无实时 API**
- **收入**：8 项资源任意组合↑
- **获取方式**：只能通过 `api_port/port` 快照突变发现
- **平板端可行性**：❌ 不可行

### 26. 维护补偿
- **UI**：无（服务器维护后自动发放）
- **API**：**无**
- **收入**：8 项资源任意组合↑
- **获取方式**：只能通过快照突变发现
- **平板端可行性**：❌ 不可行

### 27. 课金道具使用
- **UI**：使用课金购买的道具（如资源包）
- **API**：`api_req_member/payitemuse`
- **收入**：可能直接增加资源
- **脚本可读性**：⚠️ 文档标注"⭐ 课金道具"，未详细展开

### 28. 间宫/伊良湖使用
- **UI**：母港 → 舰队 → 使用间宫或伊良湖
- **API**：`api_req_member/itemuse_cond`（`api_useitem_id=54` 间宫）
- **变动**：恢复舰娘疲劳（`api_cond`），**不直接变动 8 项资源**
- **备注**：消耗的是道具，不属于 8 项资源追踪范围

### 29. 家具币变动
- **UI**：购买家具、更换家具
- **API**：`api_req_furniture/buy`、`api_req_furniture/change`
- **变动**：家具币↓（购买时）
- **备注**：家具币不在 `api_material` 的 8 项中，Resource_Account 不追踪

### 30. 新渠开放
- **UI**：工厂 → 开放新的建造渠
- **API**：`api_req_kousyou/open_new_dock`
- **变动**：可能消耗资源（文档未明确）
- **备注**：文档标注"⭐ 开渠"，未提资源消耗

### 31. 初始舰选择/新手奖励
- **UI**：账号初始化
- **API**：`api_req_init/firstship`、`api_req_init/nickname`
- **变动**：初始资源发放
- **备注**：一次性事件，Resource_Account 可忽略

---

## 四、按资源类型汇总变动场景

| 资源 | 支出场景 | 收入场景 |
|------|---------|---------|
| **燃料** | 建造、开发、补给、入渠、出击、演习、改造、改修 | 远征、任务、解体、自然回复、活动/排名/维护补偿 |
| **弹药** | 建造、开发、补给、入渠、出击、演习、改造、改修 | 远征、任务、解体、自然回复、活动/排名/维护补偿 |
| **钢材** | 建造、开发、入渠、改造、改修 | 远征、任务、解体、自然回复、活动/排名/维护补偿 |
| **铝土** | 建造、开发、**舰载机补充**、改造、改修、装备变更补充 | 远征、任务、解体、自然回复、活动/排名/维护补偿 |
| **喷火器** | 高速建造 | 远征、任务、活动/排名/维护补偿 |
| **桶** | 高速修复 | 远征、任务、活动/排名/维护补偿 |
| **开发资材** | （文档未明确直接消耗场景） | 远征、任务、活动/排名/维护补偿 |
| **螺丝** | 装备改修 | 远征、任务、活动/排名/维护补偿 |

---

## 五、平板端脚本可行性总结

| 场景 | 能否直接推云端 | 说明 |
|------|--------------|------|
| 建造投入 | ⚠️ 能（需读请求体） | 4 主资源 + 喷火器 |
| 建造产出 | ✅ 能 | 单次 API |
| 高速建造 | ✅ 能 | 喷火器固定 -1 |
| 装备开发 | ⚠️ 能（需读请求体 + 响应） | 4 主资源 |
| 舰娘解体 | ✅ 能 | 4 主资源收入，图鉴 ID 需缓存 |
| 入渠修理 | ❌ 不能 | 3 主资源消耗，需快照对比 |
| 高速修复 | ✅ 能 | 桶固定 -1 |
| 舰队补给 | ❌ 不能 | 燃料/弹药消耗，可能含铝土，需快照 |
| 舰载机补充 | ❌ 不能 | 铝土消耗，无独立 API，需快照 |
| 出击出发 | ❌ 不能 | 燃料/弹药/铝土，需快照 |
| 远征收益 | ✅ 能 | 8 项资源完美 |
| 任务报酬 | ✅ 能 | 8 项资源完美 |
| 舰娘改造 | ❓ 不确定 | 文档未完整覆盖 |
| 装备改修 | ❓ 不确定 | 螺丝消耗明确，但其他资源需确认 |
| 演习 | ❌ 不能 | 燃料/弹药少量消耗，需快照 |
| 自然回复 | ❌ 不能 | 无 API，纯推算 |
| 排名/维护补偿 | ❌ 不能 | 无 API 通知 |

---

## 六、核心结论

- **31 个场景中**，单次 API 能直接搞定推云端的约 **11 个**
- 需要请求体拦截的约 **3 个**
- 需要多 API 关联或快照对比的约 **12 个**（平板端基本做不到）
- 文档未覆盖或边缘场景约 **5 个**
- **铝土消耗**比我首次梳理时多了**舰载机补充**和**装备变更补充**两个隐藏场景，补给 API 可能同时涉及燃料/弹药/铝土三项消耗
